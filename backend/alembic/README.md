# Alembic placeholder

Initialize migrations here when the first migration is created.

Recommended next steps:

```bash
cd backend
alembic init alembic
```

Then point Alembic metadata to `app.db.session.Base.metadata`.
